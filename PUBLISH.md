
## Vector role

```
cd vector-role
git init
git branch -M main
git add .
git commit -m "Create Vector Ansible role"
git tag 1.0.0
git remote add origin git@github.com:maksimdemin18/vector-role.git
git push -u origin main
git push origin 1.0.0
```

## LightHouse role

```
cd ../lighthouse-role
git init
git branch -M main
git add .
git commit -m "Create LightHouse Ansible role"
git tag 1.0.0
git remote add origin git@github.com:maksimdemin18/lighthouse-role.git
git push -u origin main
git push origin 1.0.0
```

## Playbook

```
cd ../08-ansible-04-role-playbook
git init
git branch -M main
git add .
git commit -m "Create roles-based Ansible playbook"
git remote add origin git@github.com:maksimdemin18/08-ansible-04-role-playbook.git
git push -u origin main
```
